stages:
  - quality

quality:
  stage: quality
  tags: []
  script:
    - pwsh -NoProfile -Command "if (-not (Test-Path 'scripts/quality/run-local-quality-gates.ps1')) { throw 'Missing required script: scripts/quality/run-local-quality-gates.ps1' }; pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/quality/run-local-quality-gates.ps1 -Profile quick"
