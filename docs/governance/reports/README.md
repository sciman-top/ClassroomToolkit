# Governance Reports (Archived)

`docs/governance/reports/endstate-*.{md,json}` are historical snapshots generated by a retired governance script lane.

They are retained for traceability only and are **not** part of the active quality-gate truth source.

For current gate truth source, use:

- `docs/governance/truth-source.md`
- `scripts/quality/run-local-quality-gates.ps1`
