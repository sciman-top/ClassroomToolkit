<Window x:Class="ClassroomToolkit.App.Diagnostics.DiagnosticsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="系统兼容性诊断"
        Width="620"
        Height="420"
        MinWidth="520"
        MinHeight="340"
        WindowStartupLocation="CenterOwner"
        Topmost="True">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="SummaryText"
                   Grid.Row="0"
                   FontWeight="Bold"
                   FontSize="14"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10" />

        <TabControl Grid.Row="1">
            <TabItem Header="详情">
                <TextBox x:Name="DetailBox"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto" />
            </TabItem>
            <TabItem Header="建议">
                <TextBox x:Name="SuggestionBox"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto" />
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="复制全部" Width="90" Margin="0,0,8,0" Click="OnCopyClick" />
            <Button Content="关闭" Width="80" Click="OnCloseClick" />
        </StackPanel>
    </Grid>
</Window>
