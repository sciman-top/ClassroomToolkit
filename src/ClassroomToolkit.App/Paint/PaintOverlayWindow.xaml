<Window x:Class="ClassroomToolkit.App.Paint.PaintOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="PaintOverlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="28" ResizeBorderThickness="6" GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Border x:Name="PhotoWindowFrame" BorderBrush="{StaticResource Brush_Border_Subtle}" BorderThickness="0" Background="Transparent">
        <Grid x:Name="OverlayRoot" Background="Transparent">
            <Image x:Name="PhotoBackground" Stretch="None" SnapsToDevicePixels="True" HorizontalAlignment="Left" VerticalAlignment="Top" Visibility="Collapsed"/>
            <Image x:Name="RasterImage" Stretch="None" SnapsToDevicePixels="True" HorizontalAlignment="Left" VerticalAlignment="Top" />
            <Canvas x:Name="PreviewCanvas" IsHitTestVisible="False" />
            <Border x:Name="CustomDrawHost" Background="Transparent" IsHitTestVisible="False" />
            <Grid x:Name="PhotoControlLayer" Visibility="Collapsed" Background="{x:Null}" Panel.ZIndex="10">
                <Border x:Name="PhotoTitleBar" Height="36" Background="{StaticResource Brush_Background_L2}"
                        HorizontalAlignment="Stretch" VerticalAlignment="Top" MouseLeftButtonDown="OnPhotoTitleBarDrag">
                    <DockPanel Margin="12,0">
                        <TextBlock x:Name="PhotoTitleText" Text="图片应用" VerticalAlignment="Center"
                                   Foreground="{StaticResource Brush_Text_Primary}" FontSize="12"/>
                    </DockPanel>
                </Border>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8">
                    <Button x:Name="PhotoCloseButton" Style="{StaticResource Style_IconButton}" Click="OnPhotoMinimizeClick"
                            Width="28" Height="28" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Path Data="{StaticResource Icon_Minimize}" Fill="{StaticResource Brush_Text_Primary}" Stretch="Uniform" Width="12" Height="12"/>
                    </Button>
                </StackPanel>
                <Button x:Name="PhotoMinimizeLeftButton" Style="{StaticResource Style_IconButton}" Click="OnPhotoMinimizeClick" Width="28" Height="28"
                        HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="8">
                    <Path Data="{StaticResource Icon_Minimize}" Fill="{StaticResource Brush_Text_Primary}" Stretch="Uniform" Width="12" Height="12"/>
                </Button>
                <Button x:Name="PhotoMinimizeRightButton" Style="{StaticResource Style_IconButton}" Click="OnPhotoMinimizeClick" Width="28" Height="28"
                        HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="8">
                    <Path Data="{StaticResource Icon_Minimize}" Fill="{StaticResource Brush_Text_Primary}" Stretch="Uniform" Width="12" Height="12"/>
                </Button>
                <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="12,0,0,0">
                    <Button x:Name="PhotoPrevButtonLeft" Style="{StaticResource Style_IconButton}" Click="OnPhotoPrevClick" Width="48" Height="40" Margin="0,0,0,6" ToolTip="上一张">
                        <Path Data="{StaticResource Icon_ArrowUp}" Fill="{StaticResource Brush_Text_Primary}" Stretch="Uniform" Width="20" Height="20"/>
                    </Button>
                    <Button x:Name="PhotoNextButtonLeft" Style="{StaticResource Style_IconButton}" Click="OnPhotoNextClick" Width="48" Height="40" ToolTip="下一张">
                        <Path Data="{StaticResource Icon_ArrowDown}" Fill="{StaticResource Brush_Text_Primary}" Stretch="Uniform" Width="20" Height="20"/>
                    </Button>
                </StackPanel>
                <StackPanel Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0">
                    <Button x:Name="PhotoPrevButtonRight" Style="{StaticResource Style_IconButton}" Click="OnPhotoPrevClick" Width="48" Height="40" Margin="0,0,0,6" ToolTip="上一张">
                        <Path Data="{StaticResource Icon_ArrowUp}" Fill="{StaticResource Brush_Text_Primary}" Stretch="Uniform" Width="20" Height="20"/>
                    </Button>
                    <Button x:Name="PhotoNextButtonRight" Style="{StaticResource Style_IconButton}" Click="OnPhotoNextClick" Width="48" Height="40" ToolTip="下一张">
                        <Path Data="{StaticResource Icon_ArrowDown}" Fill="{StaticResource Brush_Text_Primary}" Stretch="Uniform" Width="20" Height="20"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
