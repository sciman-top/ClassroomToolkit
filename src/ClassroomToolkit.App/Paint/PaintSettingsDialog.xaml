<Window x:Class="ClassroomToolkit.App.Paint.PaintSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="画笔设置" Height="300" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <GroupBox Header="演示控制">
            <StackPanel Margin="8">
                <CheckBox x:Name="ControlOfficeCheck" Content="控制 PowerPoint 放映" Margin="0,4,0,0" />
                <CheckBox x:Name="ControlWpsCheck" Content="控制 WPS 放映" Margin="0,4,0,0" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="WPS 兼容" Grid.Row="1" Margin="0,12,0,0">
            <StackPanel Margin="8">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="兼容策略：" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <ComboBox x:Name="WpsModeCombo" Width="200" />
                </StackPanel>
                <CheckBox x:Name="WpsWheelCheck" Content="滚轮映射为翻页键（解决双动画/无动画）"
                          Margin="0,8,0,0" />
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="确定" Width="70" Margin="0,0,8,0" Click="OnConfirm" />
            <Button Content="取消" Width="70" Click="OnCancel" />
        </StackPanel>
    </Grid>
</Window>
