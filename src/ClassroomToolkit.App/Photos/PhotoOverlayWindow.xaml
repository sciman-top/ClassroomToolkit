<Window x:Class="ClassroomToolkit.App.Photos.PhotoOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="{x:Null}"
        ShowInTaskbar="False"
        Topmost="True"
        WindowState="Maximized"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style x:Key="Style_ModernCloseButton" TargetType="Button">
            <Setter Property="Width" Value="56"/>
            <Setter Property="Height" Value="56"/>
            <Setter Property="Background" Value="{StaticResource Brush_Background_L2}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" CornerRadius="28" Background="{TemplateBinding Background}" BorderBrush="{StaticResource Brush_Border_Light}" BorderThickness="1">
                            <Path Data="{StaticResource Icon_Close}" Fill="White" Stretch="Uniform" Width="20" Height="20" 
                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource Brush_Primary}"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Canvas x:Name="RootCanvas" SizeChanged="OnCanvasSizeChanged">
        <!-- 透明背景，点击关闭 -->
        <Rectangle x:Name="BackgroundRect" Fill="Transparent" MouseLeftButtonDown="OnCloseClick"/>

        <!-- 照片：只缩小不放大，居中显示 -->
        <Image x:Name="PhotoImage" 
               Stretch="Uniform" 
               StretchDirection="DownOnly"
               SizeChanged="OnPhotoSizeChanged"/>

        <!-- 遮挡层：用于在切换照片时隐藏旧照片 -->
        <Border x:Name="LoadingMask" Background="Transparent" Visibility="Collapsed" Panel.ZIndex="100"/>

        <!-- 学生姓名：动态定位到照片顶部 -->
        <TextBlock x:Name="NameText" 
                   Foreground="White" 
                   FontSize="56" 
                   FontWeight="Black"
                   TextAlignment="Center"
                   TextWrapping="NoWrap"
                   TextTrimming="CharacterEllipsis"
                   Panel.ZIndex="10">
            <TextBlock.Effect>
                <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.7"/>
            </TextBlock.Effect>
        </TextBlock>

        <!-- 底部关闭按钮 -->
        <Button x:Name="CloseButtonLeft" 
                Style="{StaticResource Style_ModernCloseButton}" 
                Click="OnCloseClick" 
                Opacity="0.3"/>
        <Button x:Name="CloseButtonRight" 
                Style="{StaticResource Style_ModernCloseButton}" 
                Click="OnCloseClick" 
                Opacity="0.3"/>
    </Canvas>
</Window>