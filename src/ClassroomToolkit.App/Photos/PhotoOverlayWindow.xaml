<Window x:Class="ClassroomToolkit.App.Photos.PhotoOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="{x:Null}"
        ShowInTaskbar="False"
        Topmost="True"
        WindowState="Maximized"
        ResizeMode="NoResize">
    
    <Canvas x:Name="RootCanvas" SizeChanged="OnCanvasSizeChanged">
        <!-- 透明背景，点击关闭 -->
        <Rectangle x:Name="BackgroundRect" Fill="Transparent" MouseLeftButtonDown="OnCloseClick"/>

        <!-- 照片：只缩小不放大，居中显示 -->
        <Image x:Name="PhotoImage" 
               Stretch="Uniform" 
               StretchDirection="DownOnly"
               SizeChanged="OnPhotoSizeChanged">
            <Image.Effect>
                <StaticResource ResourceKey="Shadow_Card_Subtle"/>
            </Image.Effect>
        </Image>

        <!-- 遮挡层：用于在切换照片时隐藏旧照片 -->
        <Border x:Name="LoadingMask" Background="Transparent" Visibility="Collapsed" Panel.ZIndex="100"/>

        <!-- 学生姓名：动态定位到照片顶部 -->
        <TextBlock x:Name="NameText" 
                   Foreground="White" 
                   FontSize="56" 
                   FontWeight="Black"
                   TextAlignment="Center"
                   TextWrapping="NoWrap"
                   TextTrimming="CharacterEllipsis"
                   Panel.ZIndex="10">
            <TextBlock.Effect>
                <StaticResource ResourceKey="Shadow_Floating"/>
            </TextBlock.Effect>
        </TextBlock>

        <!-- 底部关闭按钮 -->
        <Button x:Name="CloseButtonLeft"
                Style="{StaticResource Style_FullscreenShellCloseButton}"
                Click="OnCloseClick"
                Opacity="0.6"
                Width="64"
                Height="64"
                ToolTip="关闭全屏预览">
            <Path Data="{StaticResource Icon_Close}" Fill="{StaticResource Brush_Text_Primary}" Stretch="Uniform"
                  Width="{StaticResource Size_Icon_Glyph_LG}" Height="{StaticResource Size_Icon_Glyph_LG}"/>
        </Button>
        <Button x:Name="CloseButtonRight" 
                Style="{StaticResource Style_FullscreenShellCloseButton}" 
                Click="OnCloseClick" 
                Opacity="0.6"
                Width="64"
                Height="64"
                ToolTip="关闭全屏预览">
            <Path Data="{StaticResource Icon_Close}" Fill="{StaticResource Brush_Text_Primary}" Stretch="Uniform"
                  Width="{StaticResource Size_Icon_Glyph_LG}" Height="{StaticResource Size_Icon_Glyph_LG}"/>
        </Button>

        <Border x:Name="CloseHintText" Style="{StaticResource Style_FullscreenShellHintBadge}" Padding="16,8" Panel.ZIndex="10">
            <TextBlock Text="单击背景或关闭按钮即可退出" FontSize="14" Foreground="{StaticResource Brush_Text_Primary}" Opacity="0.9"/>
            <Border.Effect>
                <StaticResource ResourceKey="Shadow_Floating"/>
            </Border.Effect>
        </Border>
    </Canvas>
</Window>

