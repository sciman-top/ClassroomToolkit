<Window x:Class="ClassroomToolkit.App.RemoteKeyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="遥控键设置" Height="200" Width="360" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="预设：" VerticalAlignment="Center" Margin="0,0,8,8" />
        <ComboBox Grid.Row="0" Grid.Column="1" x:Name="PresetCombo" Margin="0,0,0,8" SelectionChanged="OnPresetChanged" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="自定义：" VerticalAlignment="Center" Margin="0,0,8,8" />
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="CustomBox" Margin="0,0,0,8" />

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Width="70" Margin="0,8,8,0" Click="OnConfirm" />
            <Button Content="取消" Width="70" Margin="0,8,0,0" Click="OnCancel" />
        </StackPanel>
    </Grid>
</Window>
