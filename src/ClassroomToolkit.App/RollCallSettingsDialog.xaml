<Window x:Class="ClassroomToolkit.App.RollCallSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="点名设置"
        Height="Auto" Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="{x:Null}"
        Topmost="True">

    <Border Background="#FAFBFF" CornerRadius="10" Margin="8" BorderBrush="#E8EEF8" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#4A90E2" BlurRadius="25" ShadowDepth="4" Opacity="0.15"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Title -->
                <RowDefinition Height="Auto"/> <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- 1. 标题栏 - 精致版 -->
            <Grid Grid.Row="0" Height="44" Background="Transparent" MouseLeftButtonDown="OnTitleBarDrag">
                <TextBlock Text="点名设置" FontWeight="SemiBold" Margin="16,0,0,0" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Primary}" FontSize="15"/>
                <Button Style="{StaticResource Style_IconButton}" Click="OnCancel" HorizontalAlignment="Right" Margin="0,0,8,0">
                    <Path Data="{StaticResource Icon_Close}" Fill="{StaticResource Brush_Text_Secondary}" Stretch="Uniform" Width="11" Height="11"/>
                </Button>
            </Grid>
            <Border Grid.Row="0" VerticalAlignment="Bottom" Height="1" Background="#E8EEF8" Margin="0,0,0,0"/>

            <!-- 2. 内容 (TabControl) - 精美版 -->
            <TabControl x:Name="SettingsTabs" Grid.Row="1" Margin="16,12,16,12" BorderThickness="0" Background="Transparent">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Padding" Value="12,6"/>
                        <Setter Property="Margin" Value="0,0,3,0"/>
                        <Setter Property="HeaderTemplate">
                            <Setter.Value>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontSize="13" FontWeight="Medium"/>
                                </DataTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                <!-- 标签页 1: 显示/照片 -->
                <TabItem Header="显示/照片">
                    <Grid Margin="8,12,8,8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="学生信息:" Margin="0,0,14,12" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,12">
                            <CheckBox x:Name="ShowIdCheck" Content="学号" Margin="0,0,18,0" FontSize="12"/>
                            <CheckBox x:Name="ShowNameCheck" Content="姓名" FontSize="12"/>
                        </StackPanel>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="照片显示:" Margin="0,0,14,12" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <CheckBox x:Name="ShowPhotoCheck" Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" FontSize="12"
                                  Checked="OnShowPhotoChanged" Unchecked="OnShowPhotoChanged" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="照片时长:" Margin="0,0,14,12" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,12">
                            <Slider x:Name="PhotoDurationSlider" Width="140" Minimum="0" Maximum="10"
                                    ValueChanged="OnPhotoDurationChanged" VerticalAlignment="Center"/>
                            <TextBlock x:Name="PhotoDurationLabel" Margin="10,0,0,0" VerticalAlignment="Center" Width="30" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        </StackPanel>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="照片文件夹:" Margin="0,0,14,0" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <ComboBox x:Name="PhotoSharedCombo" Grid.Row="3" Grid.Column="1" IsEditable="False" Height="30" FontSize="12"/>
                    </Grid>
                </TabItem>

                <!-- 标签页 2: 语音播报 -->
                <TabItem Header="语音播报">
                    <Grid Margin="8,12,8,8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="启用语音:" Margin="0,0,14,12" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <CheckBox x:Name="SpeechCheck" Grid.Row="0" Grid.Column="1" Margin="0,0,0,12" FontSize="12"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="语音引擎:" Margin="0,0,14,12" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <ComboBox x:Name="SpeechEngineCombo" Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" Height="30" FontSize="12"
                                  SelectionChanged="OnSpeechEngineChanged" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="发音人:" Margin="0,0,14,12" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <ComboBox x:Name="SpeechVoiceCombo" Grid.Row="2" Grid.Column="1" Margin="0,0,0,12" Height="30" FontSize="12"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="输出设备:" Margin="0,0,14,0" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <ComboBox x:Name="SpeechOutputCombo" Grid.Row="3" Grid.Column="1" Height="30" FontSize="12"/>
                    </Grid>
                </TabItem>

                <!-- 标签页 3: 遥控点名 -->
                <TabItem Header="遥控点名">
                    <Grid Margin="8,12,8,8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="启用遥控:" Margin="0,0,14,12" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <CheckBox x:Name="RemoteEnabledCheck" Grid.Row="0" Grid.Column="1" Margin="0,0,0,12" FontSize="12"
                                  Checked="OnRemoteEnabledChanged" Unchecked="OnRemoteEnabledChanged" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="翻页笔按键:" Margin="0,0,14,0" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <ComboBox x:Name="RemoteKeyCombo" Grid.Row="1" Grid.Column="1" IsEditable="False" Height="30" FontSize="12"/>
                    </Grid>
                </TabItem>

                <!-- 标签页 4: 倒计时提示 -->
                <TabItem Header="倒计时">
                    <Grid Margin="8,12,8,8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="结束提示:" Margin="0,0,14,12" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <CheckBox x:Name="TimerSoundCheck" Grid.Row="0" Grid.Column="1" Margin="0,0,0,12" FontSize="12"
                                  Checked="OnTimerControlChanged" Unchecked="OnTimerControlChanged" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="提示音效:" Margin="0,0,14,12" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <ComboBox x:Name="TimerSoundCombo" Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" Height="30" FontSize="12"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="中途提示:" Margin="0,0,14,12" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <CheckBox x:Name="ReminderSoundCheck" Grid.Row="2" Grid.Column="1" Margin="0,0,0,12" FontSize="12"
                                  Checked="OnTimerControlChanged" Unchecked="OnTimerControlChanged" />

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="中途音效:" Margin="0,0,14,12" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <ComboBox x:Name="ReminderSoundCombo" Grid.Row="3" Grid.Column="1" Margin="0,0,0,12" Height="30" FontSize="12"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="提示间隔:" Margin="0,0,14,0" VerticalAlignment="Center" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal">
                            <Slider x:Name="ReminderIntervalSlider" Width="140" Minimum="1" Maximum="20"
                                    ValueChanged="OnReminderIntervalChanged" VerticalAlignment="Center"/>
                            <TextBlock x:Name="ReminderIntervalLabel" Margin="10,0,0,0" VerticalAlignment="Center" Width="30" Foreground="{StaticResource Brush_Text_Secondary}" FontSize="12"/>
                        </StackPanel>
                    </Grid>
                </TabItem>
            </TabControl>

            <!-- 3. 底部按钮 - 精致版 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16,0,16,16">
                <Button Content="取消" Style="{StaticResource Style_SecondaryButton}" Width="80" Margin="0,0,10,0" Click="OnCancel"/>
                <Button Content="确定" Style="{StaticResource Style_PrimaryButton}" Width="80" Click="OnConfirm"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
