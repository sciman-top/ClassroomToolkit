<Window x:Class="ClassroomToolkit.App.RollCallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ClassroomToolkit.App.Converters"
        xmlns:behaviors="clr-namespace:ClassroomToolkit.App.Behaviors"
        Title="点名/计时" Height="360" Width="560" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis" />
    </Window.Resources>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ItemsControl Grid.Row="0" ItemsSource="{Binding Groups}"
                      Visibility="{Binding IsRollCallMode, Converter={StaticResource BoolToVis}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding}" Tag="{Binding}" Margin="4" Padding="10,4"
                            Click="OnGroupClick" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Grid.Row="1" Margin="0,12,0,12"
              Visibility="{Binding IsRollCallMode, Converter={StaticResource BoolToVis}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="#1976D2" CornerRadius="8" Margin="4">
                <TextBlock Text="{Binding CurrentStudentId}" Foreground="White" FontSize="28"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <Border Grid.Column="1" Background="#1976D2" CornerRadius="8" Margin="4">
                <TextBlock Text="{Binding CurrentStudentName}" Foreground="White" FontSize="28"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
        </Grid>

        <Border Grid.Row="1" Margin="0,12,0,12" Background="#37474F" CornerRadius="8"
                Visibility="{Binding IsRollCallMode, Converter={StaticResource InverseBoolToVis}}">
            <TextBlock Text="{Binding TimeDisplay}" Foreground="White" FontSize="36"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center"
                    Visibility="{Binding IsRollCallMode, Converter={StaticResource BoolToVis}}">
            <Button Content="点名" Margin="6" Padding="16,6" Click="OnRollClick"
                    behaviors:LongPressBehavior.Command="{Binding OpenRemoteKeyCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
            <Button Content="重置" Margin="6" Padding="16,6" Click="OnResetClick" />
            <Button Content="{Binding ModeToggleLabel}" Margin="6" Padding="16,6" Click="OnToggleModeClick" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center"
                    Visibility="{Binding IsRollCallMode, Converter={StaticResource InverseBoolToVis}}">
            <Button Content="{Binding TimerModeLabel}" Margin="4" Padding="12,6" Click="OnTimerModeClick" />
            <Button Content="{Binding StartPauseLabel}" Margin="4" Padding="12,6" Click="OnTimerStartPauseClick" />
            <Button Content="重置" Margin="4" Padding="12,6" Click="OnTimerResetClick" />
            <Button Content="设定" Margin="4" Padding="12,6" Click="OnTimerSetClick" />
            <Button Content="{Binding ModeToggleLabel}" Margin="4" Padding="12,6" Click="OnToggleModeClick" />
        </StackPanel>
    </Grid>
</Window>
